<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <style>
      body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        background-color: #f9f9f9;
        color: #333;
        padding: 0;
        margin: 0;
      }
      .container {
        max-width: 600px;
        margin: auto;
        background: #ffffff;
        padding: 30px;
        border-radius: 8px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      }
      .header {
        text-align: center;
        padding-bottom: 20px;
        border-bottom: 1px solid #ddd;
      }
      .header h1 {
        color: #2a7ae4;
        margin: 0;
      }
      .content {
        padding: 20px 0;
      }
      .token-box {
        background-color: #f1f5f9;
        padding: 20px;
        border-radius: 5px;
        text-align: center;
        font-size: 20px;
        letter-spacing: 2px;
        margin: 20px 0;
        color: #2a7ae4;
        font-weight: bold;
      }
      .reset-btn {
        display: block;
        width: 100%;
        max-width: 300px;
        margin: 20px auto;
        padding: 15px 0;
        background-color: #2a7ae4;
        color: #fff;
        text-align: center;
        border-radius: 5px;
        text-decoration: none;
        font-size: 18px;
        font-weight: bold;
        transition: background 0.2s;
      }
      .reset-btn:hover {
        background-color: #1d5fa3;
      }
      .reset-url {
        word-break: break-all;
        background: #f9fafb;
        padding: 10px;
        border-radius: 4px;
        font-size: 15px;
        margin: 10px 0 20px 0;
        color: #333;
      }
      .footer {
        text-align: center;
        margin-top: 30px;
        font-size: 13px;
        color: #888;
      }
      .footer a {
        color: #2a7ae4;
        text-decoration: none;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1>Password Reset Request</h1>
      </div>
      <div class="content">
        <p>Dear {{fullName}},</p>
        <p>
          We received a request to reset your password for your School Management System account.
        </p>
        <p>
          Please use the following options to reset your password. This link and token are valid for 1 hour.
        </p>
        <a
          class="reset-btn"
          href="{{resetUrlBase}}/reset-password?token={{resetToken}}"
          target="_blank"
        >
          Reset Password
        </a>
        <p style="text-align:center; margin: 10px 0;">or copy and paste this URL into your browser:</p>
        <div class="reset-url">
          {{resetUrlBase}}/reset-password?token={{resetToken}}
        </div>
        <p>
          If you did not request a password reset, please ignore this email or contact support.
        </p>
      </div>
      <div class="footer">
        <p>&copy; {{year}} School Management System. All rights reserved.</p>
        <p>
          Contact us: <a href="mailto:{{contactEmail}}">{{contactEmail}}</a>
        </p>
      </div>
    </div>
  </body>
</html>